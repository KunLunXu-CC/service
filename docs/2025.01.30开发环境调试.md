
## 借口调试平台

- https://studio.apollographql.com/sandbox/explorer
- 切记 api 地址写 `http://localhost:4000/api/graphql`

## 获取临时用户 token


```sh
# 执行下面命令
npm run script 
# 选择「创建临时 Token」
```

当然也可以通过线上接口的 cookie 获取到 jwt_token

## 设置请求头

cookie = `jwt_token=xxx`

